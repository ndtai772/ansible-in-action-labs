---
# The path where Drupal will be downloaded and installed
drupal_core_path: /var/www/drupal

# The resulting domain will be [domain].test
domain: drupal

# Our Drupal site name
drupal_site_name: Drupal Test